# Flask
FLASK_DEBUG=true
SECRET_KEY=change-me-super-secret
FLASK_ENV=development


# Supabase
SUPABASE_URL=https://ezhngyaqxotxcpcsdzrk.supabase.co
# For server-side writes use the SERVICE ROLE key (keep this secret). If you don't
# need server-side schema changes or RLS bypass, the anon key may work depending
# on your Row Level Security policies.
SUPABASE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY_OR_ANON_KEY
SUPABASE_SERVICE_ROLE=YOUR_SUPABASE_SERVICE_ROLE_KEY  # optional: keep and use for migrations/scripts
SUPABASE_JWT_SECRET=YOUR-JWT-SECRET

# Discord OAuth (create app in Discord Developer Portal)
DISCORD_CLIENT_ID=YOUR_CLIENT_ID
DISCORD_CLIENT_SECRET=YOUR_CLIENT_SECRET
DISCORD_REDIRECT_URI=http://127.0.0.1:5000/auth/discord/callback
DISCORD_GUILD_WEBHOOK_URL=

# AI Provider (pluggable)
LLM_PROVIDER=openai  # or "ollama"/"none"
OPENAI_API_KEY=
